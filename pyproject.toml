[project]
name = "desk-control"
version = "0.1.0"
description = ""
authors = [
    {name = "JorgenKrieger",email = "jorgen@labelnoir.me"}
]
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "bleak (>=3.0.2,<4.0.0)",
    "fastapi (>=0.141.1,<0.142.0)",
    "uvicorn (>=0.52.4,<0.53.0)"
]

# No [tool.poetry] / [build-system] here on purpose -- unlike dev/, this
# directory uses plain pip + venv (see requirements.txt), decided given how
# memory-constrained the Pi is. This file is kept only as human-readable
# dependency documentation, matching dev/'s format.
